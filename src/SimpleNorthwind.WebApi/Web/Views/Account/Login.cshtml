@model LoginViewModel
@{
    Layout = null;
    ViewData["Title"] = "登入";
    var isNameMode = Model.LoginMode != "id";
}
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>登入 · Simple Northwind</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        :root { --sn-teal: #0d9488; --sn-teal-dark: #0f766e; }
        body { background: linear-gradient(135deg, #0d9488 0%, #0f766e 100%); min-height: 100vh; }
        .login-card { max-width: 420px; border: none; border-radius: 1rem; }
        .btn-teal { background-color: var(--sn-teal); border-color: var(--sn-teal); color: #fff; }
        .btn-teal:hover { background-color: var(--sn-teal-dark); border-color: var(--sn-teal-dark); color: #fff; }
        .brand { color: var(--sn-teal); }
    </style>
</head>
<body class="d-flex align-items-center justify-content-center py-5">
    <div class="card login-card shadow-lg w-100 m-3">
        <div class="card-body p-4 p-md-5">
            <h1 class="h3 fw-bold text-center brand mb-1">Simple Northwind</h1>
            <p class="text-center text-muted mb-4">員工後台登入</p>

            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger py-2" role="alert">@Model.ErrorMessage</div>
            }
            <div asp-validation-summary="ModelOnly" class="text-danger small"></div>

            <form asp-controller="AccountUi" asp-action="Login" method="post" autocomplete="off">
                <input type="hidden" asp-for="ReturnUrl" />
                <input type="hidden" asp-for="LoginMode" id="loginMode" value="@Model.LoginMode" />

                <div class="btn-group w-100 mb-3" role="group" aria-label="登入方式">
                    <input type="radio" class="btn-check" name="modeToggle" id="modeName" autocomplete="off" @(isNameMode ? "checked" : "")>
                    <label class="btn btn-outline-secondary" for="modeName">姓名登入</label>
                    <input type="radio" class="btn-check" name="modeToggle" id="modeId" autocomplete="off" @(isNameMode ? "" : "checked")>
                    <label class="btn btn-outline-secondary" for="modeId">員工編號登入</label>
                </div>

                <div class="login-name mb-3" style="@(isNameMode ? "" : "display:none")">
                    <div class="row g-2">
                        <div class="col">
                            <label asp-for="FirstName" class="form-label"></label>
                            <input asp-for="FirstName" class="form-control" />
                        </div>
                        <div class="col">
                            <label asp-for="LastName" class="form-label"></label>
                            <input asp-for="LastName" class="form-control" />
                        </div>
                    </div>
                    <span asp-validation-for="FirstName" class="text-danger small"></span>
                    <div class="form-text">姓名區分大小寫（case-sensitive）。</div>
                </div>

                <div class="login-id mb-3" style="@(isNameMode ? "display:none" : "")">
                    <label asp-for="EmployeeId" class="form-label"></label>
                    <input asp-for="EmployeeId" class="form-control" inputmode="numeric" />
                    <span asp-validation-for="EmployeeId" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Password" class="form-label"></label>
                    <input asp-for="Password" class="form-control" />
                    <span asp-validation-for="Password" class="text-danger small"></span>
                </div>

                <div class="form-check mb-3">
                    <input asp-for="RememberMe" class="form-check-input" />
                    <label asp-for="RememberMe" class="form-check-label"></label>
                </div>

                <button type="submit" class="btn btn-teal w-100 py-2 fw-semibold">登入</button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/tz.js"></script>
    <script>
        (function () {
            var modeInput = document.getElementById("loginMode");
            var nameGroup = document.querySelector(".login-name");
            var idGroup = document.querySelector(".login-id");
            function apply(mode) {
                modeInput.value = mode;
                nameGroup.style.display = mode === "id" ? "none" : "";
                idGroup.style.display = mode === "id" ? "" : "none";
            }
            document.getElementById("modeName").addEventListener("change", function () { apply("name"); });
            document.getElementById("modeId").addEventListener("change", function () { apply("id"); });
        })();
    </script>
</body>
</html>
